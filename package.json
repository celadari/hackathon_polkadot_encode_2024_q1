{
    "name": "@devphase/examples",
    "private": true,
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "deploy": "ts-node --transpile-only -r tsconfig-paths/register ./scripts/deploy.ts",
        "test": "devphase test"
    },
    "dependencies": {
        "arg": "^5.0.2"
    },
    "devDependencies": {
        "@devphase/cli": "^0.7.1",
        "@devphase/service": "^0.7.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.4.2"
    }
}
